<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}DonkeyTour{% endblock %}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body class="font-[Inter] bg-[#fcfaf7] text-stone-700 antialiased selection:bg-[#4A90A4] selection:text-white {% if is_granted('ROLE_ADMIN') %}overflow-x-hidden{% endif %}">

{% if app.user %}
    {# ========================================== #}
    {# LAYOUT CONDICIONAL SEGÚN ROL               #}
    {# ========================================== #}

    {# Fondo general decorativo #}
    <div class="fixed -bottom-24 -right-24 w-96 h-96 bg-[#4A90A4] rounded-full blur-3xl opacity-10 pointer-events-none z-0"></div>

    {% if is_granted('ROLE_ADMIN') %}
        {# --- VISTA ADMINISTRADOR: Layout fijo con Sidebar --- #}
        <div class="flex h-screen w-full relative">
            <aside class="w-72 hidden md:flex flex-col bg-white/80 backdrop-blur-xl border-r border-stone-100 shadow-[20px_0_50px_rgba(139,94,60,0.03)] z-20">
                <div class="h-24 flex items-center px-8 border-b border-stone-100/50">
                    <div class="relative group flex items-center gap-3">
                        <div class="absolute -inset-4 bg-gradient-to-tr from-amber-200 to-transparent rounded-full blur-xl opacity-40 group-hover:opacity-60 transition duration-500"></div>
                        <span class="text-3xl relative">🫏</span>
                        <span class="font-serif font-extrabold text-[#3E2F28] text-xl tracking-tight relative">DonkeyTour</span>
                    </div>
                </div>

                <nav class="flex-1 px-4 py-8 space-y-2 overflow-y-auto">
                    <p class="px-4 text-[10px] font-bold uppercase tracking-[0.2em] text-stone-400 mb-4 mt-2">Principal</p>
                    <a href="{{ path('app_home') }}" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-[#8B5E3C] bg-[#8B5E3C]/10 font-bold transition-all duration-300 group">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" /></svg>
                        <span>Home</span>
                    </a>
                    <a href="#" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-stone-500 font-medium hover:bg-stone-100/80 hover:text-[#4A90A4] transition-all duration-300 group">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 group-hover:scale-110 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" /></svg>
                        <span>Reservas Globales</span>
                    </a>
                    <a href="#" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-stone-500 font-medium hover:bg-stone-100/80 hover:text-[#4A90A4] transition-all duration-300 group">
                        <span class="text-lg grayscale opacity-70 group-hover:grayscale-0 group-hover:opacity-100 transition-all duration-300 group-hover:scale-110">🐴</span>
                        <span>Gestión de Burros</span>
                    </a>

                    <p class="px-4 text-[10px] font-bold uppercase tracking-[0.2em] text-stone-400 mb-4 mt-8">Administración</p>
                    <a href="#" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-stone-500 font-medium hover:bg-stone-100/80 hover:text-[#8B5E3C] transition-all duration-300 group">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 group-hover:scale-110 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" /></svg>
                        <span>Admin usuarios</span>
                    </a>
                    <a href="#" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-stone-500 font-medium hover:bg-stone-100/80 hover:text-[#8B5E3C] transition-all duration-300 group">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 group-hover:rotate-90 transition-transform duration-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" /><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" /></svg>
                        <span>Configuración</span>
                    </a>
                </nav>
            </aside>

            <div class="flex-1 flex flex-col min-w-0 z-10">
                <header class="h-32 bg-white/40 backdrop-blur-md border-b border-stone-100 flex items-center justify-between px-8 z-20">
                    <div>
                        <h2 class="text-xl font-bold text-[#3E2F28]">{% block page_title %}Panel Principal{% endblock %}</h2>
                    </div>
                    {# Perfil User/Logout común - lo extraemos a un macro si fuera necesario, o lo copiamos #}
                    <div class="flex items-center gap-6">
                        <div class="flex items-center gap-3">
                            <div class="text-right hidden md:block">
                                <p class="text-sm font-bold text-[#3E2F28]">{{ app.user.nombre | default('Admin') }}</p>
                                <p class="text-xs text-stone-400 font-medium">Administrador</p>
                            </div>
                            <div class="w-11 h-11 rounded-2xl bg-[#4A90A4]/10 text-[#4A90A4] border-[#4A90A4]/20 flex items-center justify-center font-bold text-lg border shadow-sm">
                                {{ app.user.nombre|default('A')|first|upper }}
                            </div>
                        </div>
                        <a href="{{ path('app_logout') }}" class="p-2.5 rounded-xl text-stone-400 hover:text-red-500 hover:bg-red-50 transition-all duration-300" title="Cerrar sesión">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" /></svg>
                        </a>
                    </div>
                </header>
                <main class="flex-1 p-8 overflow-y-auto overflow-x-hidden">
                    {% block body %}{% endblock %}
                </main>
            </div>
        </div>

    {% else %}
        {# --- VISTA CLIENTE: Layout natural con scroll --- #}
        <div class="min-h-screen flex flex-col relative z-10">
            <header class="h-28 bg-white/80 backdrop-blur-md border-b border-stone-100 sticky top-0 flex items-center justify-between px-8 z-50">
                <div class="flex items-center gap-3">
                    <img src="{{ asset('img/logoDonkey_sinFondo.png') }}" alt="DonkeyTour" class="size-28 object-contain">
                </div>

                <nav class="hidden md:flex items-center gap-8">
                    <a href="{{ path('app_home') }}" class="text-[#8B5E3C] font-bold hover:text-[#764f32] transition-colors">Inicio</a>
                    <a href="#sobre-nosotros" class="text-stone-500 font-medium hover:text-[#4A90A4] transition-colors">Sobre Nosotros</a>
                    <a href="{{ path('app_service_index') }}" class="text-stone-500 font-medium hover:text-[#4A90A4] transition-colors">Servicios</a>
                    <a href="{{ path('app_donkey_index') }}" class="text-stone-500 font-medium hover:text-[#4A90A4] transition-colors">Nuestros Burros</a>
                </nav>

                <div class="flex items-center gap-6">
                    <div class="flex items-center gap-3">
                        <div class="text-right hidden md:block">
                            <p class="text-sm font-bold text-[#3E2F28]">{{ app.user.nombre | default('Viajero') }}</p>
                            <a href="#" class="text-xs text-[#4A90A4] font-medium hover:underline">Mis Reservas</a>
                        </div>
                        <div class="w-10 h-10 rounded-full bg-[#8B5E3C]/10 text-[#8B5E3C] border-[#8B5E3C]/20 flex items-center justify-center font-bold text-lg border shadow-sm">
                            {{ app.user.nombre|default('V')|first|upper }}
                        </div>
                    </div>
                    <a href="{{ path('app_logout') }}" class="p-2.5 rounded-xl text-stone-400 hover:text-red-500 hover:bg-red-50 transition-all duration-300" title="Cerrar sesión">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" /></svg>
                    </a>
                </div>
            </header>

            <main class="flex-1 w-full">
                {# Para el cliente, inyectamos la vista y dejamos que haga scroll libre #}
                {% block client_body %}{% endblock %}
            </main>

            <footer class="bg-white border-t border-stone-100 py-8 mt-12 text-center text-stone-400 text-xs tracking-wider uppercase">
                &copy; {{ "now"|date("Y") }} DonkeyTour - Granada, España
            </footer>
        </div>
    {% endif %}

{% else %}
    {# --- VISTA LOGIN/REGISTER --- #}
    {% block public_body %}{% endblock %}
{% endif %}

</body>
</html>
