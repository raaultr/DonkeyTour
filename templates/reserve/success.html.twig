{% extends 'base.html.twig' %}

{% block title %}{% if reserve.service.isSponsorship %}¡Apadrinamiento Confirmado!{% else %}¡Reserva Confirmada!{% endif %} · DonkeyTour{% endblock %}

{% block client_body %}
<div class="max-w-2xl mx-auto px-6 py-24 text-center">

    {# Animación de éxito #}
    <div class="mb-8">
        <div class="w-24 h-24 mx-auto bg-green-100 rounded-full flex items-center justify-center mb-6 animate-bounce">
            <svg class="w-12 h-12 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
            </svg>
        </div>
        {% if reserve.service.isSponsorship %}
            <h1 class="text-4xl font-bold text-[#3E2F28] mb-3">¡Apadrinamiento confirmado!</h1>
            <p class="text-stone-400 text-lg">Gracias por apadrinar a {{ reserve.selectedDonkey ? reserve.selectedDonkey.nombre : 'nuestro burro' }}. ¡Tu generosidad marca la diferencia!</p>
        {% else %}
            <h1 class="text-4xl font-bold text-[#3E2F28] mb-3">¡Reserva confirmada!</h1>
            <p class="text-stone-400 text-lg">Tu aventura con DonkeyTour está reservada. ¡Nos vemos pronto!</p>
        {% endif %}
    </div>

    {# Resumen breve #}
    <div class="bg-white/80 backdrop-blur-xl border border-stone-100 rounded-3xl p-8 shadow-sm text-left mb-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 text-sm">
            <div>
                <p class="text-[10px] font-bold uppercase tracking-widest text-stone-400 mb-1">
                    {% if reserve.service.isSponsorship %}Nº de apadrinamiento{% else %}Nº de reserva{% endif %}
                </p>
                <p class="font-bold text-[#3E2F28] text-lg">#{{ reserve.id }}</p>
            </div>
            {% if not reserve.service.isSponsorship %}
            <div>
                <p class="text-[10px] font-bold uppercase tracking-widest text-stone-400 mb-1">Fecha</p>
                <p class="font-bold text-[#3E2F28] text-lg">{{ reserve.reserveDate ? reserve.reserveDate|date('d/m/Y H:i') ~ 'h' : '—' }}</p>
            </div>
            {% endif %}
            <div>
                <p class="text-[10px] font-bold uppercase tracking-widest text-stone-400 mb-1">Servicio</p>
                <p class="font-bold text-[#3E2F28]">{{ reserve.service.type|capitalize }}</p>
            </div>
            <div>
                <p class="text-[10px] font-bold uppercase tracking-widest text-stone-400 mb-1">
                    {% if reserve.service.isSponsorship %}Burro apadrinado{% else %}Burro asignado{% endif %}
                </p>
                <p class="font-bold text-[#3E2F28]">{{ reserve.selectedDonkey ? reserve.selectedDonkey.nombre : '—' }} 🐴</p>
            </div>
        </div>

        <div class="mt-6 pt-6 border-t border-stone-100">
            <div class="flex items-center gap-3 text-amber-600 bg-amber-50 px-4 py-3 rounded-xl border border-amber-100 text-sm">
                <svg class="w-5 h-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                <span>La pasarela de pago no está disponible todavía. El pago se realizará in situ.</span>
            </div>
        </div>
    </div>

    {# Acciones #}
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a href="{{ path('app_reserve_my') }}"
           class="px-8 py-3 bg-[#8B5E3C] hover:bg-[#764f32] text-white font-bold rounded-2xl transition-all shadow-lg shadow-[#8B5E3C]/20">
            Ver mis reservas
        </a>
        <a href="{{ path('app_home') }}"
           class="px-8 py-3 border-2 border-stone-200 text-stone-600 font-bold rounded-2xl hover:border-stone-300 hover:bg-stone-50 transition-all">
            Volver al inicio
        </a>
    </div>
</div>
{% endblock %}
