{% extends 'base.html.twig' %}

{% block title %}Nuestros Protagonistas - DonkeyTour{% endblock %}

{% block client_body %}
<div class="bg-[#FCFAF7] min-h-screen">

    {# --- HERO SECTION: LOS PROTAGONISTAS --- #}
    <div class="relative h-[70vh] flex items-center justify-center bg-stone-900 overflow-hidden">
        <img src="{{ asset('img/burroBanner.png') }}" class="absolute inset-0 w-full h-full object-cover opacity-40" alt="Nuestros Burros">
        <div class="relative z-20 text-center px-6">
            <span class="inline-block px-4 py-1 bg-[#764F32] text-white text-[10px] font-black uppercase tracking-[0.3em] mb-6">Conoce a la familia</span>
            <h1 class="text-5xl md:text-7xl font-bold text-white mb-4 tracking-tighter">Nuestros Burros</h1>
            <p class="text-lg text-stone-200 max-w-2xl mx-auto font-medium opacity-90">Los verdaderos guías de Granada. Criados con respeto y amor.</p>
        </div>
    </div>

    {# --- ÁREA DE GESTIÓN (Solo visible si necesitas el botón de crear) --- #}
    <div class="max-w-7xl mx-auto px-6 mt-12 flex justify-end">
        <a href="{{ path('app_donkey_new') }}" class="px-6 py-3 bg-[#764F32] text-white text-xs font-bold uppercase tracking-widest rounded-xl hover:bg-stone-800 transition-all shadow-lg">
            + Registrar nuevo burro
        </a>
    </div>

    {# --- GRID DE BURROS (Estilo Moderno) --- #}
    <section class="max-w-7xl mx-auto px-6 py-16">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12">
            {% for donkey in donkeys %}
                <div class="group bg-white rounded-[2.5rem] overflow-hidden border border-stone-100 shadow-sm hover:shadow-2xl transition-all duration-500 flex flex-col">
                    
                    {# Imagen (Usando photoUrl si existe, si no banner por defecto) #}
                    <div class="relative h-80 overflow-hidden bg-stone-200">
                        <img src="{{ donkey.photoUrl ? donkey.photoUrl : asset('img/burroBanner.png') }}" 
                             class="w-full h-full object-cover transition-transform duration-1000 group-hover:scale-110" 
                             alt="{{ donkey.nombre }}">
                        
                        {# Badge de Disponibilidad #}
                        <div class="absolute top-6 left-6">
                            {% if donkey.disponible %}
                                <span class="bg-green-500 text-white px-4 py-1.5 rounded-full text-[10px] font-black uppercase tracking-widest shadow-sm">
                                    Disponible
                                </span>
                            {% else %}
                                <span class="bg-red-500 text-white px-4 py-1.5 rounded-full text-[10px] font-black uppercase tracking-widest shadow-sm">
                                    En descanso
                                </span>
                            {% endif %}
                        </div>
                    </div>

                    {# Datos del Burro #}
                    <div class="p-10 text-center flex-grow flex flex-col">
                        <span class="text-[#764F32] text-[10px] font-black uppercase tracking-[0.3em] mb-2 block">{{ donkey.race }}</span>
                        <h3 class="text-3xl font-bold text-stone-900 mb-6 group-hover:text-[#764F32] transition-colors">
                            {{ donkey.nombre }}
                        </h3>
                        
                        {# Info Técnica (Years, Kilogram, MaxWeightr) #}
                        <div class="grid grid-cols-3 gap-4 py-6 border-y border-stone-50 mb-8">
                            <div class="text-center">
                                <span class="block text-stone-300 text-[9px] font-black uppercase mb-1 italic">Edad</span>
                                <span class="text-stone-800 font-bold text-sm">{{ donkey.years }} años</span>
                            </div>
                            <div class="text-center border-x border-stone-50">
                                <span class="block text-stone-300 text-[9px] font-black uppercase mb-1 italic">Peso</span>
                                <span class="text-stone-800 font-bold text-sm">{{ donkey.kilogram }}kg</span>
                            </div>
                            <div class="text-center">
                                <span class="block text-stone-300 text-[9px] font-black uppercase mb-1 italic">Carga Máx</span>
                                <span class="text-stone-800 font-bold text-sm">{{ donkey.maxWeightr }}kg</span>
                            </div>
                        </div>

                        {# Acciones #}
                        <div class="mt-auto flex gap-3">
                            <a href="{{ path('app_donkey_show', {'id': donkey.id}) }}" class="flex-1 py-4 bg-[#764F32] text-white font-black text-[10px] uppercase tracking-widest hover:bg-stone-800 transition-all rounded-xl">
                                Ver Perfil
                            </a>
                            <a href="{{ path('app_donkey_edit', {'id': donkey.id}) }}" class="px-5 py-4 border border-stone-200 text-stone-400 hover:text-[#764F32] hover:border-[#764F32] transition-all rounded-xl text-xs">
                                ⚙️
                            </a>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-span-full text-center py-20 bg-white rounded-3xl border-2 border-dashed border-stone-200">
                    <p class="text-stone-400 italic">No hay burros registrados en el establo.</p>
                </div>
            {% endfor %}
        </div>
    </section>

    {# --- SECCIÓN INFORMATIVA (Más contenido) --- #}
    <div class="bg-[#301633] py-20 text-white">
        <div class="max-w-4xl mx-auto px-6 text-center">
            <h2 class="text-3xl font-bold mb-6 italic">Comprometidos con el Bienestar Animal</h2>
            <p class="text-stone-400 leading-relaxed">
                Nuestros burros tienen un límite estricto de carga de **{{ donkeys|length > 0 ? donkeys[0].maxWeightr : '80' }}kg** para garantizar que siempre se sientan cómodos. 
                Cada animal tiene su propio historial médico y tiempos de descanso monitorizados.
            </p>
        </div>
    </div>
</div>
{% endblock %}